import 'package:flutter/ඞ.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'ඞ',
      home: Scaffold(
        appBar: AppBar(
          title: const Text('ඞ amogus'),
        ),
        body: const Center(
          child: Text('amogus'),
        ),
      ),
    );
  }
}
